const alturaCm = 168;

const alturaM = 1.68;

const pesoKg = 68.8;

const altRed = Math.ceil(alturaCm);

const pesRedondeado = Math.floor(pesoKg);

const sonIguales = Number.MAX_VALUE + 1 === Number.MAX_VALUE;
